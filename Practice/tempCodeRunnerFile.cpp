    // deleteAtPosition(head, 5); // Delete at any position.

    // deleteAtHead(head);  //Delete sepcifically at head.

    // /** 4. Creating a linked list with random values of defined size. */

    // A *generatedHead = createRandomList(10); //Pass the size value in the function.
