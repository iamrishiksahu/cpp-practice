#include <bits/stdc++.h>
#include <iostream>
#include<bits/stdc++>

using namespace std;

int main()
{


    vector<int> p;
    fill(p.begin(), p.end(), 0);

    for (int i = 0; i < p.size(); i++)
    {
        cout << p[i];
    }

    return 0;
}